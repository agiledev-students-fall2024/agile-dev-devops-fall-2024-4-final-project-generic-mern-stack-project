import React from 'react';

const MainContent = () => {
    return (
        <main>
            {/* Add content based on your wireframe */}
            <h2>Welcome to the App!</h2>
            <p>This is where the main content will go.</p>
        </main>
    );
};

export default MainContent;